services:
  sift:
    build: .
    container_name: sift
    env_file: .env
    volumes:
      - ./workspaces:/app/workspaces
      - ./site:/app/site
    # Override CMD to run full pipeline
    command: ["run"]
